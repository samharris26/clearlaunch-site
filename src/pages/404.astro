---
import Layout from "../layouts/Layout.astro";
---

<Layout title="404 – Page Not Found | ClearLaunch">
  <div class="flex flex-col min-h-screen">
    <!-- Navigation -->
    <header
      class="border-b border-slate-800/50 backdrop-blur-md sticky top-0 z-50 bg-slate-950/80"
    >
      <div
        class="max-w-5xl mx-auto flex items-center justify-between gap-6 py-4 px-4 sm:px-6"
      >
        <!-- Logo -->
        <a href="/" class="flex items-center gap-3 hover:opacity-80 transition-opacity">
          <img
            src="/ClearLaunch-logo.svg"
            alt="ClearLaunch"
            class="h-9 w-auto"
          />
        </a>

        <!-- Nav Links -->
        <nav class="hidden md:flex items-center gap-8 text-sm font-medium">
          <a
            href="/#how-it-works"
            class="text-slate-400 hover:text-sky-400 transition-colors"
            >How it works</a
          >
          <a
            href="/#features"
            class="text-slate-400 hover:text-sky-400 transition-colors"
            >Features</a
          >
          <a
            href="/#pricing"
            class="text-slate-400 hover:text-sky-400 transition-colors"
            >Pricing</a
          >
        </nav>

        <!-- CTA -->
        <div class="flex items-center gap-4">
          <a
            href="/#hero-form"
            class="inline-flex items-center gap-2 rounded-full bg-slate-50 text-slate-950 text-sm font-semibold tracking-tight px-4 py-2 hover:bg-white hover:scale-105 transition-all shadow-lg shadow-slate-50/10"
          >
            Get early access
          </a>
        </div>
      </div>
    </header>

    <!-- 404 Content -->
    <main class="flex-1 flex items-center justify-center px-4 py-20">
      <div class="max-w-2xl mx-auto text-center space-y-8">
        <!-- Background accents -->
        <div class="absolute inset-0 -z-10 overflow-hidden pointer-events-none">
          <div
            class="absolute top-1/4 left-1/4 h-[500px] w-[800px] rounded-full bg-indigo-500/10 blur-[100px]"
          ></div>
          <div
            class="absolute bottom-1/4 right-1/4 h-[400px] w-[600px] rounded-full bg-sky-500/10 blur-[80px]"
          ></div>
        </div>

        <!-- Icon -->
        <div class="flex justify-center">
          <div class="flex h-24 w-24 items-center justify-center rounded-2xl border border-slate-800/50 bg-slate-900/50 backdrop-blur-sm">
            <i data-lucide="rocket" class="h-12 w-12 text-sky-400"></i>
          </div>
        </div>

        <!-- 404 Number -->
        <h1 class="text-7xl sm:text-8xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-sky-400 via-indigo-400 to-emerald-400">
          404
        </h1>

        <!-- Heading -->
        <h2 class="text-3xl sm:text-4xl font-bold text-slate-50">
          Page not found
        </h2>

        <!-- Description -->
        <p class="text-lg sm:text-xl text-slate-400 max-w-xl mx-auto leading-relaxed">
          Looks like this launch plan got lost in space. The page you're looking for doesn't exist or has been moved.
        </p>

        <!-- Actions -->
        <div class="flex flex-col sm:flex-row items-center justify-center gap-4 pt-4">
          <a
            href="/"
            class="inline-flex items-center gap-2 rounded-full bg-gradient-to-r from-sky-500 to-indigo-500 px-6 py-3 text-sm font-semibold text-white shadow-lg shadow-sky-500/20 hover:scale-105 transition-all"
          >
            <i data-lucide="home" class="h-4 w-4"></i>
            Go to Homepage
          </a>
          <a
            href="/#hero-form"
            class="inline-flex items-center gap-2 rounded-full border border-slate-800 bg-slate-900/50 backdrop-blur-sm px-6 py-3 text-sm font-medium text-slate-300 hover:bg-slate-800/50 hover:text-slate-50 transition-all"
          >
            <i data-lucide="arrow-left" class="h-4 w-4"></i>
            Get Started
          </a>
        </div>
      </div>
    </main>
  </div>
</Layout>
